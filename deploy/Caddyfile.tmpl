# 由 oracle-cloud-init.sh / update.sh 產生：{IP} 會換成主機的公用 IP
# Let's Encrypt 自 2026/01 起可直接為 IP 簽發憑證（需 shortlived 設定檔，6 天效期，Caddy 自動續簽）
{IP} {
	tls {
		issuer acme {
			dir https://acme-v02.api.letsencrypt.org/directory
			profile shortlived
		}
	}
	encode gzip
	reverse_proxy 127.0.0.1:8000
}
